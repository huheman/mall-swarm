spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:8848
        namespace: dev
      config:
        server-addr: http://nacos-registry:8848
        file-extension: yaml
        namespace: dev