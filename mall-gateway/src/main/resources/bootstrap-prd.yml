spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://www.huhp.cc:8848
        namespace: prd
      config:
        server-addr: http://www.huhp.cc:8848
        file-extension: yaml
        namespace: prd
logging:
  level:
    root: info